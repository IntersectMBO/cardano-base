{
    "$id": "https://raw.githubusercontent.com/IntersectMBO/cardano-base/master/cardano-config/schemas/Tracing.schema.json",
    "$schema": "http://json-schema.org/draft-07/schema#",
    "description": "TracingConfiguration",
    "properties": {
        "HermodTracing": {
            "description": "Tracing configuration as a path to a separate file holding it. Consumed by the node tracing system (trace-dispatcher), not parsed or validated by cardano-config.",
            "format": "path",
            "title": "HermodTracing",
            "type": "string"
        }
    },
    "title": "Tracing",
    "type": "object"
}
